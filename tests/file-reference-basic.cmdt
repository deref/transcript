# Test basic file reference functionality

# Create a binary file with a specific name
$ echo -e "binary\x01\x02\x03data" > custom.bin

# Reference it with a custom filename (manually created)
$ cat custom.bin  
1< 001.bin

# Clean up
$ rm custom.bin
